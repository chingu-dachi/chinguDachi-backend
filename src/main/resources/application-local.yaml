spring:
  application:
    name: chinguDachi

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    open-in-view: false
  docker:
    compose:
      lifecycle-management: start-and-stop

cors:
  allowed-origin: "http://localhost:3000"